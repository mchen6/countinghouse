1. DeviceConfig support in CEAMS web device package management
2. Better device app health monitor support (CPU, memory, crash / thread exit log write to deviceLog, and etc), and better indicator of device app's healthy state on CEAMS web
3. Better msgQueue implementation for worker thread
4. Better distributed support (etcd, app transition, recovering and etc)
